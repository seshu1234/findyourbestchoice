{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php -d display_errors=1 -d error_reporting=E_ALL artisan serve --host=0.0.0.0 --port=${PORT} 2>&1"
  }
}
